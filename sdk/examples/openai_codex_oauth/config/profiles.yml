spec: flatprofiles
spec_version: "2.5.0"

data:
  model_profiles:
    codex:
      provider: openai-codex
      backend: codex
      name: gpt-5.3-codex
      oauth:
        provider: openai-codex
        auth_file: ~/.agents/flatmachines/auth.json
        originator: flatmachines

  default: codex
