spec: flatagent
spec_version: "2.5.0"

data:
  name: codex-oauth-checker
  model: codex

  system: |
    You are a concise diagnostic assistant.

  user: |
    {{ input.task }}

metadata:
  description: "Placeholder codex-backed agent config for oauth diagnostics"
