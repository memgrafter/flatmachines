# OpenAI Codex OAuth Diagnostics (Python)

Diagnostics-only example: resolves auth file and optionally validates that OAuth credentials exist.
No model/API call is made.

## Run

```bash
cd sdk/examples/openai_codex_oauth/python
./run.sh --local
./run.sh --local --require-credential
```
