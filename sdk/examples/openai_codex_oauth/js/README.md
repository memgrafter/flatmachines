# OpenAI Codex OAuth Diagnostics (JavaScript)

Diagnostics-only example: resolves auth file and optionally validates OAuth credential presence.
No model/API call is made.

## Run

```bash
cd sdk/examples/openai_codex_oauth/js
./run.sh --local
./run.sh --local --require-credential
```
