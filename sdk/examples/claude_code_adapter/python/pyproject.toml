[project]
name = "claude-code-example"
version = "0.1.0"
description = "Example: Claude Code CLI driven by FlatMachine"
requires-python = ">=3.10"
dependencies = [
    "flatmachines[flatagents]",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
