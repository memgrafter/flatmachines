# Tool Loop Example (Python)

Standalone `ToolLoopAgent` demo using shared config in `../config/agent.yml`.

## Run

```bash
cd sdk/examples/tool_loop/python
./run.sh --local
./run.sh --local "What time is it in Tokyo?"
```
