spec: flatagent
spec_version: "4.0.0"
data:
  prompt: ../prompts/verify.prompt.yml
  profile: ../profiles/codex-readonly.profile.yml
metadata:
  description: Verify the warmed Codex thread can recall the build hash
